#include <stdio.h>

/**
 * main - print main function
 *
 * Description:: main function
 * this program prints "programming of lower case in reverse
 * Return: 0
 */
int main(void)
{
char(ch);
for (ch = 'z'; ch >= 'a'; ch--)
	putchar(ch);
putchar('\n');
return (0);
}
