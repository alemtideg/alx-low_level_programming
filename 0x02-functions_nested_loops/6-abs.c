#include "main.h"

/**
 *  _abs - Computes the absolute value of an integer.
 *  @n: The integer to ibe computed
 *
 *  Return: 0
 */
int_abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
