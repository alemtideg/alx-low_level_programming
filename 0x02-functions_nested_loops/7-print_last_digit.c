#include "main.h"

/**
 * print_last_digit - print the last digit of number N
 * @n:last digit of n
 * Return: the lat digit of n
 */
int print_last_digit(int);
{
	int last_digit = n % 10;

	if (last_digit < 0)
		last digit * = -1;

	_putchar(last_digit + '0');

	return (last_digit);
}
